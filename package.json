{
  "scripts": {
    "build": "BASE='/' pnpm build:docs && BASE='/foo/' pnpm build:docs",
    "build:docs": "vitepress build docs",
    "serve": "serve dist/docs"
  },
  "devDependencies": {
    "serve": "^13.0.2",
    "vitepress": "1.0.0-alpha.1",
    "vue": "^3.2.37"
  }
}
